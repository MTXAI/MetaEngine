1. 音频播放时存在卡顿：可能由于 AudioContainer 没有检测数字人说话状态，说话过程中（语音 frame 已经成功生产），不应该生产空白语音帧
2. 音频播放完成后，存在噪声：问题 1 解决后，再观察
3. track sync 目前会带来卡顿：sync_queue 会导致 video 和 audio 互相等待，导致互相影响。暂时去掉这个功能
4. tts 是否需要流式输入，tts 是否需要流式输出，agent 是否需要流式输出，需要明确：当前的架构，流式输入和流式输出，导致语音和画面过于卡顿，并且支持流式输入和输出会带来很高的复杂度。暂时终止流式支持，并且去掉相关代码，保持代码整洁
5. tts 流式输入的情况下，只输入标点符号，或断句，导致语音怪异：去掉流式支持
6. 知识库 agent 响应时长过长：去掉 web 搜索

